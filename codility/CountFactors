function solution(N) {
    // write your code in JavaScript (Node.js 8.9.4)
    let i = 1;
    let answer = 0;
    while(i*i<N) {
        if(N%i == 0) {
            answer = answer + 2;
        }
        i= i+1;
    }
    if(i*i == N) {
        answer = answer + 1;
    }
    return answer;
}
